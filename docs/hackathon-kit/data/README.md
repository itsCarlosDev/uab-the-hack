# Documentos oficiales del kit

El kit original incluye `rookie_filtered_aps.json` y `rookie_filtered_clients.json`, ambos mayores de 100 MB. Descarga esos ficheros desde el enlace oficial (ver `onedrive.txt`) y colocalos aqui cuando necesites replicar el entorno tal y como lo entrego la UAB.

No se versionan para evitar romper los limites de GitHub.