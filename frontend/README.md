# Frontend legacy data

Algunos cuadernos y scripts antiguos siguen buscando `frontend/rookie_filtered_aps.json` y `frontend/rookie_filtered_clients.json`. Copia aqui las versiones que generas en `data/processed/rookie/` cuando necesites ese flujo, pero no los incluyas en Git porque superan los 100 MB.